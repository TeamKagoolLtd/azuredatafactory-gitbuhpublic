name: ADF-CopyActivity
on:
  push:
    branches: [ "develop" ]
  workflow_dispatch:
jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: copyactivity/checkout@v3
    - name: copy_activity
      uses: TeamKagoolLtd/azuredatafactory-gitbuhpublic@develop
      with:
        source: "tests/a.txt"
        target: "test/a.txt"
